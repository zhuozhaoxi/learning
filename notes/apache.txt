options配置
	

1、AllowOverride
通常利用Apache的rewrite模块对 URL 进行重写的时候， 
rewrite规则会写在 .htaccess 文件里。
但要使 apache 能够正常的读取.htaccess 文件的内容，就必须对.htaccess 所在目录进行配置。

2、Allow from all
 //设置允许所有人访问
 //也可以设置成Allow from 127.0.0.1

3、Order allow,deny  //匹配顺序为先允许，后拒绝

4、Require all granted  //对这个目录给予授权


